#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../aws-ses-local/build/server.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../aws-ses-local/build/server.js" "$@"
  ret=$?
fi
exit $ret
